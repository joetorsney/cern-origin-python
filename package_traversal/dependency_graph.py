import json

def build_dependency_graph(filename: str) -> dict:
    """Given a json file, reads the dependencies into a dictionary representing the graph"""

    # Remove everything to test github actions
    return None
